version: '3'

services:
  app:
    build:
      context: .
    volumes:
      - ./outputs/data/:/app/outputs/data
      - ./outputs/logs/:/app/outputs/logs
    env_file:
      - app/.env
    tty: true
    entrypoint: python yearly_data.py 